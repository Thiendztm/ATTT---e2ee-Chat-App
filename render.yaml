services:
  - type: web
    name: moxi-chat-api
    env: node
    rootDir: .
    buildCommand: npm install
    startCommand: npm start
    plan: free
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: "production"
      - key: PORT
        value: "3000"

databases: []
